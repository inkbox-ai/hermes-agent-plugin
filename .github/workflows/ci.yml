name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # The inkbox SDK is intentionally not installed here: the tests mock it,
      # and the pinned inkbox>=0.4.10 (with the harness= signup kwarg) isn't on
      # PyPI yet. Install only the deps the tests actually import.
      - name: Install test deps
        run: pip install pytest aiohttp

      - name: Test
        run: pytest -q
