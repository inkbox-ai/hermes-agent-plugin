# Inkbox Plugin Installed

Run:

```bash
pip install inkbox aiohttp
hermes plugins enable inkbox
hermes inkbox setup
hermes inkbox doctor
hermes gateway run
```

The setup wizard stores Inkbox values in `~/.hermes/.env`.
